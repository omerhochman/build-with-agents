# Manifest of every secret/config the code reads — the single source of
# truth for "what must be set". Code reads env from day one; values are
# requested via blocked-by-human.md, never chat or commits. Secret names
# follow docs/PROVIDERS.md. Add a line here in the same PR that adds the
# env read.
