# Product

<!-- template: the source doc — every other doc derives from this one. Keep
     it opinionated and short; this is positioning, not a requirements list. -->

## One-liner

**⟨The product in one sentence a stranger repeats correctly after hearing it
once.⟩**

<!-- template: then decompose it — name the core primitive/interaction and
     how it extends. The strongest shape is one mechanic with swappable
     payloads: the cheapest payload is the no-signup viral wedge, a stickier
     payload is the retention layer that earns accounts, and later payloads
     are cheap adds on the same primitive. If the idea doesn't decompose
     that way, say what the wedge and the retention layer are instead. -->

⟨Core mechanic, and the wedge → retention → cheap-adds sequence it enables.⟩

## Why now

<!-- template: 2–4 bullets of evidence the niche is open: what existing
     alternatives get wrong (ads, price, platform locks, complexity), what
     platform/tech shift newly enables this. End with the one-bold-line
     statement of the open niche. -->

- ⟨What the free alternatives get wrong.⟩
- ⟨What the polished alternatives cost or lock out.⟩
- ⟨What newly makes this possible or cheap.⟩

**⟨The open niche, in one bold sentence.⟩**

## MVP scope

<!-- template: one subsection per phase. Phase 0 is the wedge: the cheapest
     shippable thing that proves the core interaction — aggressively cut
     anything needing accounts, backend, or a privacy policy. For each
     phase: what it is, why it's ordered here, then a hard In/Out list.
     "Out: everything else" is the most useful line in this doc. -->

### v0 — ⟨wedge, in five words⟩

⟨What it is, the target ship time, and why this is the cheapest true test
of the core interaction.⟩

**In:**
- ⟨minimum feature list⟩

**Out:** everything else.

### v1.0 — ⟨next phase⟩

⟨What it adds and what it exists to enable — often the vehicle for the
retention layer rather than a goal in itself.⟩

### v1.1 — ⟨retention layer⟩

⟨The layer that earns accounts/commitment, and why it's safe to build now
that the wedge proved the interaction.⟩
